name: Build & Deploy Booking CRM

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ------ Backend ------
      - name: Setup Node.js for backend
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Build and pack backend
        working-directory: backend
        run: |
          npm ci
          npm run build
          echo "🔍 Checking what files exist after build:"
          ls -la
          echo "🔍 Checking dist directory:"
          ls -la dist/ || echo "❌ dist directory not found"
          echo "🔍 Creating archive with files:"
          ls -la dist package.json package-lock.json
          tar czf ../backend.tar.gz dist package.json package-lock.json
          echo "🔍 Archive contents:"
          tar -tzf ../backend.tar.gz
      - name: Upload backend artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: backend.tar.gz

      # ------ Frontend ------
      - name: Setup Node.js for frontend
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Inject production env for frontend
        run: |
          cat > frontend/.env.production << 'EOF'
          REACT_APP_API_URL=${{ secrets.FRONTEND_API_URL }}
          EOF

      - name: Build and pack frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build
          tar czf ../frontend.tar.gz build
      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend.tar.gz

  deploy:
    needs: build
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest

    steps:
      - name: Download backend artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-build
          path: backend-build

      - name: Download frontend artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: frontend-build

      - name: Flatten artifacts
        run: |
          mv backend-build/backend.tar.gz ./backend.tar.gz
          mv frontend-build/frontend.tar.gz ./frontend.tar.gz

      - name: Copy artifacts to EC2
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: backend.tar.gz,frontend.tar.gz
          target: ${{ secrets.EC2_DEPLOY_PATH }}

      - name: Deploy on EC2 via SSH
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd ${{ secrets.EC2_DEPLOY_PATH }}

            sudo chown -R $USER:$USER .

            # --- Backend deployment ---
            # Backup .env files before cleaning
            if [ -f "backend/.env.production" ]; then
              cp backend/.env.production /tmp/.env.production.backup
            fi

            # Clean old backend files (except public/ which contains uploaded files)
            if [ -d "backend" ]; then
              find backend -mindepth 1 -not -path "backend/public" -not -path "backend/public/*" -delete
            fi

            mkdir -p backend
            mkdir -p backend/public/images
            sudo chown -R $USER:$USER backend/public/images
            sudo chmod -R 777 backend/public/images
            tar xzf backend.tar.gz -C backend --strip-components=1

            # Restore .env.production if it existed
            if [ -f "/tmp/.env.production.backup" ]; then
              cp /tmp/.env.production.backup backend/.env.production
              cp backend/.env.production backend/.env
              rm /tmp/.env.production.backup
            fi
            cd backend
            npm install --omit=dev
            pm2 delete booking-backend || true
            pm2 start dist/server.js \
              --name booking-backend \
              --cwd ${{ secrets.EC2_DEPLOY_PATH }}/backend \
              --env production
            cd ..

            # --- Frontend deployment ---
            rm -rf frontend
            mkdir -p frontend
            tar xzf frontend.tar.gz -C frontend --strip-components=1

            # --- Clean up archives ---
            rm -f backend.tar.gz frontend.tar.gz
